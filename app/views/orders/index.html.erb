<h2>All Orders</h2>

<% @orders.each do |order| %>
  <p><%= order.name %></p>
  <p><%= order.email %></p>
  <p><%=order.line_item%></p>
  <ul>
    <% order.line_items.each do |item| %>
    <li><%= item.quantity %> x <%= item.product.name %></li>
    <% end %>
  </ul>
<% end